info:
  id: hubspot-api-key
  name: HubSpot API Key
  author: Fábio Castro
  description: List of common regular expression patterns to detect HubSpot API keys in HTTP request bodies.

  classification:
    tags:
      - exposure
      - apikey
      - hubspot

  references:
    - "HubSpot API documentation"

  matchers:
    - pattern: "hapikey=[a-z0-9]{32}"
      type: body
      description: "Matches HubSpot API Key in request bodies."

  response:
    - status_code: 200
